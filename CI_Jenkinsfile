pipeline {
	agent any
	
	stages {
		stage('Build') {
			steps {
				echo 'Building...'
				nodejs(nodeJSInstallationName: 'NodeJs'{
                    sh 'npm config ls'
                    sh 'npm install'
                }
			}
		}
	}
}